#include<stdio.h>
#include"header.h"
int main()
{
    int num;
    printf("Enter num:");scanf("%d",&num);
    /*if(strong(num))
    printf("Strong");
    else
    printf("not strong");*/
    // strongnum(num);
    
    return 0;
}